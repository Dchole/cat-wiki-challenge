overwrite: true
schema: "src/apollo/schema.gql"
documents: null
generates:
  src/apollo/types/generated/server.d.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
